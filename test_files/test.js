import React from 'react';
const fs = require('fs');

class Component {
    constructor() {
        this.state = {};
    }
    
    render() {
        return null;
    }
}

function regularFunction(x, y) {
    return x + y;
}

const arrowFunc = (a, b) => a * b;

export default Component;
